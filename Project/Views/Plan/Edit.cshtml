@using GymPlanner.Application.Models.Plan;
@model PlanEditDto

<form method="post" asp-action="Edit">
    <div class="border b3">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="row pt-4">
            <div class="col-6">
                <h2 class="p1-3">Редактирование плана</h2>
            </div>
            <div class="col-6 text-right">
                <a asp-action="Index" class="btn btn-primary w-50">Назад</a>
            </div>
        </div>

        <br /><br />

        <div class="row">
            <div class="col-16">
                <table class="table table-bordered border-primary" style="width:100%">
                    <thead>
                        <tr>
                            <td>Упражнения/Дни</td>
                            @for (int i = 0; i < Model.ExerciseFrequencies.Count; i++)
                            {
                                <td>@Model.ExerciseFrequencies[i].FrequencyName</td>
                            }
                        </tr>
                    </thead>
                    <tbody>
                        @for (int i = 0; i < Model.ExerciseFrequencies.Count; i++)
                        {
                            <tr>
                                <td>@Model.ExerciseFrequencies[i].ExerciseName</td>
                                <td><input asp-for="@Model.ExerciseFrequencies[i].Description" /></td>
                            </tr>
                        }
                    </tbody>
                </table>
                <div class="row">
                    <div class="col-12">
                        <button type="button" id="addRow" class="btn btn-secondary">Добавить упражнение</button>
                        <button type="button" id="addColumn" class="btn btn-secondary">Добавить частоту</button>
                    </div>
                </div>

                <div class="form-group row">
                    <div class="col-8 offset-4 row">
                        <div class="col">
                            <input type="submit" class="btn btn-info w-100" value="Обновить" />
                        </div>
                        <div class="col">
                            <a asp-action="Index" class="btn btn-success w-100">Назад</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>

@section Scripts{
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            var addRowButton = document.getElementById('addRow');
            var addColumnButton = document.getElementById('addColumn');
            var table = document.querySelector('.table');
            var rowCount = table.rows.length;
            var columnCount = table.rows[0].cells.length;

            addRowButton.addEventListener('click', function () {
                var newRow = table.insertRow(-1);
                var newCell = newRow.insertCell(0);
                newCell.innerHTML = '<input placeholder="Упражнение" name="ExerciseFrequencies[' + (rowCount - 1) + '].ExerciseName" />';

                for (var i = 1; i < columnCount; i++) {
                    var newCell = newRow.insertCell(i);
                    newCell.innerHTML = '<input type="text" placeholder="Описание" name="ExerciseFrequencies[' + ((i - 1) + ((rowCount - 1) * (columnCount - 1))) + '].Description" />';
                }

                rowCount++;
            });

            addColumnButton.addEventListener('click', function () {
                var headerRow = table.rows[0];
                var newHeaderCell = headerRow.insertCell(-1);
                newHeaderCell.innerHTML = '<input type="text" placeholder="Частота" name="ExerciseFrequencies[' + (columnCount - 1) + '].FrequencyName" />';

                for (var i = 1; i < rowCount; i++) {
                    var newCell = table.rows[i].insertCell(-1);
                    newCell.innerHTML = '<input type="text" placeholder="Описание" name="ExerciseFrequencies[' + ((i - 1) + ((rowCount - 1) * (columnCount - 1))) + '].Description" />';
                }

                columnCount++;
            });
        });

    </script>
}